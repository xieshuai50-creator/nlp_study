python classify_llm.py --num_samples 200

==================================================
Zero-Shot LLM 分类结果（Qwen2-0.5B-Instruct）
==================================================
  样本数   : 200
  准确率   : 72/200 = 0.3600
  无法解析 : 58 条 (29.0%)
  总耗时   : 9.1s, 均值 0.05s/条

对比参考（典型结果）：
  BERT fine-tune (3 epochs, cls)   val accuracy ≈ 0.57 ~ 0.62
  Qwen2-0.5B zero-shot             val accuracy ≈ 0.36

思考题：
  1. LLM zero-shot 在哪些类别上表现好？为什么？
  2. fine-tuning 数据量越多效果越好吗？试试只用 1000 条训练数据。
  3. 如果换成 few-shot（每类给 2 个示例），准确率会提升多少？

结果已保存 → /root/text_classification/outputs/llm_zero_shot_results.json


# 思考题：
1. LLM zero-shot 在哪些类别上表现好？为什么？
教育、体育等，因为特征明显，区分度高。
2. fine-tuning 数据量越多效果越好吗？试试只用 1000 条训练数据。
    不是，可能过拟合。
3. 如果换成 few-shot（每类给 2 个示例），准确率会提升多少？
    每个类别给出1个示例后，准确率为0.33。按道理会升高，实际略有下降。我感觉真实标签有的有问题，模型预测的反而是对的。
